# ============================================================
#  arXiv Topic Discovery — Python Dependencies
#  Tested on Python 3.10.x (recommended)
#  Compatible with Python 3.9.x and 3.11.x
#  Install: pip install -r requirements.txt
# ============================================================

# ── Core Data Handling ───────────────────────────────────────
numpy==1.24.4
pandas==2.0.3
scipy==1.11.4

# ── Machine Learning ────────────────────────────────────────
scikit-learn==1.3.2          # TF-IDF, NMF, t-SNE, train/test split

# ── Topic Modelling ─────────────────────────────────────────
gensim==4.3.2                # LDA, CoherenceModel, Dictionary, BOW corpus

# ── Natural Language Processing ─────────────────────────────
nltk==3.8.1                  # Stopwords, WordNetLemmatizer, tokenisation

# ── Visualisation ───────────────────────────────────────────
matplotlib==3.7.5
seaborn==0.12.2
pyLDAvis==3.4.1              # Interactive LDA topic explorer
ipywidgets==8.1.1            # Interactive widgets inside notebooks

# ── Progress & Utilities ────────────────────────────────────
tqdm==4.66.1                 # Progress bars during preprocessing
joblib==1.3.2                # Parallel processing (used internally by sklearn)

# ── Jupyter Environment ─────────────────────────────────────
jupyter==1.0.0
jupyterlab==4.0.9
nbconvert==7.11.0            # Headless notebook execution
ipykernel==6.26.0

# ── File I/O ────────────────────────────────────────────────
pyarrow==14.0.1              # Fast parquet/feather read-write (optional but recommended)
